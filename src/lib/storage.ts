/** localStorage key used to persist all editor state.
 *  Bump the version suffix when default layout geometry changes so existing
 *  users start from the new defaults instead of stale saved state. */
export const STORAGE_KEY = "lavolta_bcb_v7";
